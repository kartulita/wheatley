(function (angular, _) {
	'use strict';
	/**
	 * @ngdoc module
	 * @module parsers
	 * @requires underscore
	 */

	angular.module('battlesnake.parsers', []);

	if (!_) {
		throw new Error('Underscore.js required');
	}

})(window.angular, window._);
