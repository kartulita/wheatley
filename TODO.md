Look at putting a smaller, simpler build system into each module folder, to allow
per-module customisations to be made, then have the global build system combine
the results of these (via recursive-make).

Browserify?
